package com.example.library.source.repository;

import com.example.library.source.entity.WebSource;
import com.example.library.source.repository.SourceRepository;

public interface WebSourceRepository extends SourceRepository<WebSource> {
}
